{
  "name": "weilimo-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Wildcard gateway: <name>.wei.limo serves the IPFS contenthash of <name>.wei, read live from the WNS contract.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node ipns.test.mjs && node onchain.test.mjs && node handler.test.mjs",
    "deploy:worker": "wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  }
}
